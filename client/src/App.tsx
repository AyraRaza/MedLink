// MedLink quiet-luxury SaaS system: warm canvas, black rail, soft pastel signals, calm operational motion.
import { createContext, useContext, useEffect, useRef, useState } from "react";
import {
  Activity, AlertCircle, ArrowLeft, Bell, Building2, ChevronRight, CircleHelp,
  ClipboardList, Database, Droplets, LayoutDashboard, LogOut, Menu, Package,
  Pencil, Plus, Search, Settings, ShieldCheck, SlidersHorizontal, Sparkles,
  Stethoscope, Syringe, X, Zap, FileCheck2, Globe2
} from "lucide-react";
import { toast } from "sonner";
import { apiClient, getApiErrorMessage } from "@/lib/api";
import { clearAccessToken, getAccessToken, getCurrentUser, login, registerUser, type User } from "@/lib/auth";

const logo = "/assets/medlink-logo.svg";
const networkArt = "/assets/medlink-network.svg";
const opsArt = "/assets/medlink-operations.svg";
const authArt = "/assets/medlink-auth.svg";

type View = "dashboard" | "inventory" | "resource" | "add" | "edit" | "organization" | "settings" | "discover" | "discovery-resource" | "requests" | "request" | "request-create" | "login" | "onboarding";
type OrganizationData = { id: number; name: string; registration_number: string; organization_type: string; email: string | null; phone: string | null; address: string | null; city: string | null; state: string | null; pincode: string | null; verification_status: "PENDING" | "VERIFIED" | "REJECTED"; is_active: boolean; created_at: string; updated_at: string };
type ResourceCategory = "EQUIPMENT" | "MEDICINE" | "BLOOD" | "MEDICAL_SUPPLY" | "OTHER";
type AvailabilityStatus = "AVAILABLE" | "LOW_STOCK" | "OUT_OF_STOCK" | "UNAVAILABLE";
type ResourceCondition = "NEW" | "GOOD" | "USED" | "DAMAGED";
type ResourceData = { id: number; organization_id: number; name: string; category: ResourceCategory; description: string | null; quantity: number; unit: string; availability_status: AvailabilityStatus; condition: ResourceCondition; expiry_date: string | null; is_active: boolean; created_at: string; updated_at: string };
type ResourceDiscoveryData = { id: number; name: string; category: ResourceCategory; description: string | null; quantity: number; unit: string; availability_status: AvailabilityStatus; condition: ResourceCondition; expiry_date: string | null; provider_organization_id: number; provider_organization_name: string; provider_organization_type: string; provider_city: string | null; provider_state: string | null };
type RequestStatus = "PENDING" | "APPROVED" | "REJECTED" | "CANCELLED" | "FULFILLED" | "RECEIVED";
type ResourceRequestData = { id: number; resource_id: number; resource_name: string; requesting_organization_id: number; requesting_organization_name: string; providing_organization_id: number; providing_organization_name: string; requested_quantity: number; message: string | null; status: RequestStatus; created_at: string; updated_at: string };
type ResourceDraft = { name: string; category: ResourceCategory | ""; description: string; quantity: string; unit: string; availability_status: AvailabilityStatus | ""; condition: ResourceCondition | ""; expiry_date: string };

const organizationTypeValues: Record<string, OrganizationData["organization_type"]> = {
  Hospital: "HOSPITAL",
  Clinic: "CLINIC",
  "Diagnostic Center": "DIAGNOSTIC_CENTER",
  "Blood Bank": "BLOOD_BANK",
  Other: "OTHER",
};

const organizationTypeLabels: Record<string, string> = {
  HOSPITAL: "Hospital",
  CLINIC: "Clinic",
  DIAGNOSTIC_CENTER: "Diagnostic Center",
  BLOOD_BANK: "Blood Bank",
  OTHER: "Other",
};

function organizationTypeLabel(value: string) { return organizationTypeLabels[value] || value; }
function verificationLabel(value: OrganizationData["verification_status"]) { return value[0] + value.slice(1).toLowerCase(); }
function userDisplayName(user: User | null) { return user?.full_name.trim() || user?.email.split("@")[0] || "Account"; }
function userInitials(user: User | null) { return userDisplayName(user).split(/\s+/).map((part) => part[0]).join("").slice(0, 2).toUpperCase(); }
function userRoleLabel(user: User | null) { return user?.role === "admin" ? "Administrator" : "Hospital staff"; }
function formatCurrentDate() { return new Date().toLocaleDateString(undefined, { weekday: "long", month: "long", day: "numeric", year: "numeric" }).toUpperCase(); }
function formatCurrentTime() { return new Date().toLocaleTimeString(undefined, { hour: "2-digit", minute: "2-digit" }); }
const AccountContext = createContext<{ user: User | null; organization: OrganizationData | null }>({ user: null, organization: null });
function useAccount() { return useContext(AccountContext); }

type MockResource = { name: string; category: string; quantity: string; unit: string; status: string; condition: string; expiry?: string; icon: any };
const dashboardResources: MockResource[] = [];
const resources = dashboardResources;

const categoryLabels: Record<ResourceCategory, string> = { EQUIPMENT: "Equipment", MEDICINE: "Medicine", BLOOD: "Blood", MEDICAL_SUPPLY: "Medical Supply", OTHER: "Other" };
const availabilityLabels: Record<AvailabilityStatus, string> = { AVAILABLE: "Available", LOW_STOCK: "Low stock", OUT_OF_STOCK: "Out of stock", UNAVAILABLE: "Unavailable" };
const conditionLabels: Record<ResourceCondition, string> = { NEW: "New", GOOD: "Good", USED: "Used", DAMAGED: "Damaged" };
const categoryOptions = Object.entries(categoryLabels) as [ResourceCategory, string][];
const availabilityOptions = Object.entries(availabilityLabels) as [AvailabilityStatus, string][];
const conditionOptions = Object.entries(conditionLabels) as [ResourceCondition, string][];

function resourceIcon(category: ResourceCategory) { return category === "BLOOD" ? Droplets : category === "MEDICINE" ? Syringe : category === "MEDICAL_SUPPLY" ? Package : category === "OTHER" ? Database : category === "EQUIPMENT" ? Activity : Stethoscope; }
function resourceStatus(status: AvailabilityStatus) { return status.replace("_", " "); }
function formatExpiry(value: string | null) { return value ? new Date(value).toLocaleDateString(undefined, { month: "short", year: "numeric" }) : "Not applicable"; }
function dateInputValue(value: string | null) { return value ? value.slice(0, 10) : ""; }
function resourceToDraft(resource?: ResourceData): ResourceDraft { return { name: resource?.name || "", category: resource?.category || "", description: resource?.description || "", quantity: resource ? String(resource.quantity) : "", unit: resource?.unit || "", availability_status: resource?.availability_status || "", condition: resource?.condition || "", expiry_date: dateInputValue(resource?.expiry_date || null) }; }

const nav = [
  { id: "dashboard", label: "Dashboard", icon: LayoutDashboard },
  { id: "organization", label: "Organization", icon: Building2 },
  { id: "inventory", label: "Inventory", icon: ClipboardList },
  { id: "resource", label: "Resources", icon: Database },
  { id: "discover", label: "Discover Resources", icon: Globe2 },
  { id: "requests", label: "My Requests", icon: FileCheck2 },
  { id: "settings", label: "Settings", icon: Settings },
] as const;

function StatusPill({ status, label = status }: { status: string; label?: string }) {
  const styles: Record<string, string> = {
    AVAILABLE: "bg-[#e6f3eb] text-[#397154]",
    "LOW STOCK": "bg-[#fff0d9] text-[#956a2b]",
    "OUT OF STOCK": "bg-[#f3e8e9] text-[#98666b]",
    UNAVAILABLE: "bg-[#ececed] text-[#6d6d71]",
    VERIFIED: "bg-[#e6f3eb] text-[#397154]",
    PENDING: "bg-[#fff0d9] text-[#956a2b]",
    APPROVED: "bg-[#dce9f0] text-[#4f7183]",
    REJECTED: "bg-[#f3e8e9] text-[#98666b]",
    CANCELLED: "bg-[#ececed] text-[#6d6d71]",
    FULFILLED: "bg-[#dce9f0] text-[#4f7183]",
    RECEIVED: "bg-[#e6f3eb] text-[#397154]",
  };
  return <span className={`status-pill ${styles[status] || "bg-[#ececed] text-[#666]"}`}><span className="status-dot" />{label}</span>;
}

function Sidebar({ view, setView, mobileOpen, setMobileOpen, onLogout, resourceCount, resourcesLoading }: { view: View; setView: (v: View) => void; mobileOpen: boolean; setMobileOpen: (v: boolean) => void; onLogout: () => void; resourceCount: number; resourcesLoading: boolean }) {
  const { user, organization } = useAccount();
  return <>
    <aside className={`sidebar ${mobileOpen ? "mobile-open" : ""}`}>
      <div className="brand" onClick={() => { setView("dashboard"); setMobileOpen(false); }}>
        <div className="brand-mark"><img src={logo} alt="" /></div><span>MEDLINK</span>
      </div>
      <div className="workspace"><div className="workspace-avatar">{organization?.name.slice(0, 2).toUpperCase() || "--"}</div><div><strong>{organization?.name || "Organization"}</strong><small>{organization ? organizationTypeLabel(organization.organization_type) : "Loading"}</small></div><ChevronRight size={15} /></div>
      <div className="nav-group"><small className="nav-caption">WORKSPACE</small>{nav.map(item => { const Icon = item.icon; const isChildView = (view === "add" || view === "edit") && item.id === "inventory" || view === "discovery-resource" && item.id === "discover" || view === "request" && item.id === "requests"; return <button key={item.id} className={`nav-item ${view === item.id || isChildView ? "active" : ""}`} onClick={() => { setView(item.id); setMobileOpen(false); }}><Icon size={18} /><span>{item.label}</span>{item.id === "inventory" && !resourcesLoading && <span className="nav-count">{resourceCount}</span>}</button> })}</div>
      <div className="rail-note"><Sparkles size={16} /><p><strong>Keep critical resources in view.</strong><br />Your inventory is in good standing.</p></div>
      <div className="sidebar-bottom"><button className="nav-item" onClick={() => toast.info("Help center is coming soon.")}><CircleHelp size={18} /><span>Help center</span></button><button className="nav-item" onClick={onLogout}><LogOut size={18} /><span>Log out</span></button><div className="rail-user"><div className="avatar">{userInitials(user)}</div><div><strong>{userDisplayName(user)}</strong><small>{userRoleLabel(user)}</small></div><Settings size={15} /></div></div>
    </aside>
    {mobileOpen && <div className="mobile-scrim" onClick={() => setMobileOpen(false)} />}
  </>;
}

function Topbar({ title, setView, setMobileOpen, searchQuery = "", setSearchQuery }: { title: string; setView: (v: View) => void; setMobileOpen: (v: boolean) => void; searchQuery?: string; setSearchQuery?: (value: string) => void }) {
  const { user, organization } = useAccount();
  return <header className="topbar"><div className="mobile-menu" onClick={() => window.dispatchEvent(new Event("medlink:open-nav"))}><Menu size={21} /></div><div><p className="eyebrow">{organization?.name || "Organization"} / {formatCurrentTime()}</p><h1>{title}</h1></div><div className="top-actions"><div className="searchbox"><Search size={17} /><input placeholder="Search resources" value={searchQuery} onChange={(event) => setSearchQuery?.(event.target.value)} /></div><button className="icon-button" onClick={() => toast.info("You are all caught up.")}><Bell size={18} /><span className="notification-dot" /></button><button className="top-profile" onClick={() => setView("settings")}><div className="avatar">{userInitials(user)}</div><span>{userDisplayName(user)}</span><ChevronRight size={15} /></button></div></header>;
}

function Metric({ label, value, note, tone, icon: Icon }: any) { return <div className={`metric-card ${tone}`}><div className="metric-top"><span>{label}</span><div className="metric-icon"><Icon size={17} /></div></div><div className="metric-value">{value}</div><div className="metric-note">{note}</div></div>; }

function Dashboard({ resources, organization, loading, error, setView, setSelectedResourceId }: { resources: ResourceData[]; organization: OrganizationData | null; loading: boolean; error: string | null; setView: (v: View) => void; setSelectedResourceId: (id: number) => void }) {
  const totalResources = loading || error ? 0 : resources.length;
  const availableResources = loading || error ? 0 : resources.filter((resource) => resource.availability_status === "AVAILABLE").length;
  const lowStockResources = loading || error ? 0 : resources.filter((resource) => resource.availability_status === "LOW_STOCK").length;
  const outOfStockResources = loading || error ? 0 : resources.filter((resource) => resource.availability_status === "OUT_OF_STOCK").length;
  const activeCategories = loading || error ? 0 : new Set(resources.map((resource) => resource.category)).size;
  const recentResources = [...resources].sort((left, right) => new Date(right.updated_at).getTime() - new Date(left.updated_at).getTime()).slice(0, 4);
  const openResource = (resource: ResourceData) => { setSelectedResourceId(resource.id); setView("resource"); };
  const { user } = useAccount();
    return <div className="page"><Topbar title={`Good morning, ${userDisplayName(user)}`} setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">{formatCurrentDate()}</p><h2>Here’s your resource pulse.</h2><p className="muted">A clear view of what your teams have, need, and can act on next.</p></div><button className="button primary" onClick={() => setView("add")}><Plus size={17} /> Add resource</button></div><div className="metrics-grid"><Metric label="Total resources" value={String(totalResources)} note={`Across ${activeCategories} categories`} tone="lilac" icon={Database} /><Metric label="Available" value={String(availableResources)} note="Active resource records" tone="mint" icon={ShieldCheck} /><Metric label="Low stock" value={String(lowStockResources)} note="Needs attention" tone="peach" icon={AlertCircle} /><Metric label="Out of stock" value={String(outOfStockResources)} note="Review this week" tone="blue" icon={Zap} /></div><div className="main-grid"><section className="card recent-card"><div className="card-heading"><div><p className="section-kicker">LIVE INVENTORY</p><h3>Recent resources</h3></div><button className="text-button" onClick={() => setView("inventory")}>View inventory <ChevronRight size={15} /></button></div><div className="resource-list">{loading ? <p className="muted">Loading inventory...</p> : error ? <p className="muted">{error}</p> : recentResources.length === 0 ? <p className="muted">{organization?.name || "Your organization"} has no active resources yet.</p> : recentResources.map((resource, i) => <ResourceRow key={resource.id} resource={resource} onClick={() => openResource(resource)} delay={i} />)}</div></section><section className="overview-card"><div className="overview-copy"><div><p className="section-kicker">ORGANIZATION OVERVIEW</p><h3>{organization?.name || "Your organization"}<br /><em>in control.</em></h3></div><span className="overview-badge"><Activity size={13} /> Syncing</span></div><div className="overview-art"><img src={networkArt} alt="Abstract connected healthcare network" /></div><div className="overview-foot"><div><strong>{activeCategories}</strong><span>active categories</span></div><div><strong>98.4%</strong><span>data completeness</span></div><button onClick={() => setView("organization")}>Organization profile <ChevronRight size={15} /></button></div></section></div></main></div>;
  return <div className="page"><Topbar title="Good morning, Jordan" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">TUESDAY, AUGUST 30, 2026</p><h2>Here’s your resource pulse.</h2><p className="muted">A clear view of what your teams have, need, and can act on next.</p></div><button className="button primary" onClick={() => setView("add")}><Plus size={17} /> Add resource</button></div><div className="metrics-grid"><Metric label="Total resources" value="128" note="Across 5 categories" tone="lilac" icon={Database} /><Metric label="Available" value="96" note="75% of inventory" tone="mint" icon={ShieldCheck} /><Metric label="Low stock" value="08" note="Needs attention" tone="peach" icon={AlertCircle} /><Metric label="Out of stock" value="02" note="Review this week" tone="blue" icon={Zap} /></div><div className="main-grid"><section className="card recent-card"><div className="card-heading"><div><p className="section-kicker">LIVE INVENTORY</p><h3>Recent resources</h3></div><button className="text-button" onClick={() => setView("inventory")}>View inventory <ChevronRight size={15} /></button></div><div className="resource-list">{resources.slice(0, 4).map((r, i) => <ResourceRow key={r.name} resource={r} onClick={() => setView("resource")} delay={i} />)}</div></section><section className="overview-card"><div className="overview-copy"><div><p className="section-kicker">ORGANIZATION OVERVIEW</p><h3>Connected, current,<br /><em>in control.</em></h3></div><span className="overview-badge"><Activity size={13} /> Syncing</span></div><div className="overview-art"><img src={networkArt} alt="Abstract connected healthcare network" /></div><div className="overview-foot"><div><strong>5</strong><span>active categories</span></div><div><strong>98.4%</strong><span>data completeness</span></div><button onClick={() => setView("organization")}>Organization profile <ChevronRight size={15} /></button></div></section></div><div className="bottom-grid"><section className="card status-card"><div className="card-heading"><div><p className="section-kicker">ORGANIZATION STATUS</p><h3>Verified organization</h3></div><StatusPill status="VERIFIED" /></div><p className="muted">Ardent Health Network is verified for biomedical resource management.</p><div className="progress-line"><span style={{ width: "100%" }} /></div><div className="status-meta"><span>Verification complete</span><span>100%</span></div></section><section className="card quick-card"><img src={opsArt} alt="Medical inventory supplies" /><div><p className="section-kicker">QUICK ACTION</p><h3>Keep your catalog current.</h3><button className="text-button" onClick={() => setView("add")}>Add a resource <ChevronRight size={15} /></button></div></section></div></main></div>;
}

function ResourceRow({ resource: r, onClick, delay = 0 }: { resource: MockResource | ResourceData; onClick: () => void; delay?: number }) { const isBackendResource = "id" in r; const Icon = isBackendResource ? resourceIcon(r.category) : r.icon; const category = isBackendResource ? categoryLabels[r.category] : r.category; const quantity = isBackendResource ? String(r.quantity) : r.quantity; const status = isBackendResource ? resourceStatus(r.availability_status) : r.status; return <button className="resource-row" style={{ animationDelay: `${delay * 40}ms` }} onClick={onClick}><div className="resource-icon"><Icon size={17} /></div><div className="resource-name"><strong>{r.name}</strong><span>{category}</span></div><div className="resource-qty"><strong>{quantity}</strong><span>{r.unit}</span></div><StatusPill status={status} label={isBackendResource ? availabilityLabels[r.availability_status] : status} /><ChevronRight size={16} className="row-arrow" /></button>; }

function Inventory({ setView }: { setView: (v: View) => void }) { const [filter, setFilter] = useState("All resources"); return <div className="page"><Topbar title="Biomedical inventory" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">RESOURCE CATALOG / 128 ITEMS</p><h2>Everything in one view.</h2><p className="muted">Manage availability, condition, and expiry across your organization.</p></div><button className="button primary" onClick={() => setView("add")}><Plus size={17} /> Add resource</button></div><div className="inventory-toolbar"><div className="filter-chips">{["All resources", "Equipment", "Medicine", "Blood", "Medical Supply"].map(x => <button key={x} className={filter === x ? "selected" : ""} onClick={() => setFilter(x)}>{x}</button>)}</div><button className="filter-button" onClick={() => toast.info("More filters are coming soon.")}><SlidersHorizontal size={16} /> Filters</button></div><section className="card inventory-card"><div className="inventory-head"><span>Resource</span><span>Quantity</span><span>Status</span><span></span></div>{resources.filter(r => filter === "All resources" || r.category === filter).map((r, i) => <ResourceRow key={r.name} resource={r} onClick={() => setView("resource")} delay={i} />)}</section></main></div>; }

function ResourceDetail({ setView }: { setView: (v: View) => void }) { const r = resources[0]; return <div className="page"><Topbar title="Resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content detail-page"><button className="back-button" onClick={() => setView("inventory")}><ArrowLeft size={16} /> Back to inventory</button><div className="detail-heading"><div><div className="detail-icon"><Activity size={22} /></div><p className="section-kicker">EQUIPMENT / RESOURCE #ML-0041</p><h2>{r.name}</h2><p className="muted">Last updated today at 08:35 AM by Jordan Davis</p></div><div className="detail-actions"><StatusPill status="AVAILABLE" /><button className="button secondary" onClick={() => toast.success("Edit mode is ready in the full app.")}><Pencil size={16} /> Edit resource</button></div></div><div className="detail-stats"><div className="card detail-stat"><span>Quantity</span><strong>18</strong><small>units available</small></div><div className="card detail-stat"><span>Condition</span><strong>Excellent</strong><small>Last inspected Aug 12</small></div><div className="card detail-stat"><span>Active status</span><strong>Active</strong><small>Visible to organization</small></div><div className="card detail-stat"><span>Organization</span><strong>Ardent Health</strong><small>Healthcare Network</small></div></div><div className="detail-columns"><section className="card detail-description"><p className="section-kicker">DESCRIPTION</p><h3>Reliable infusion support for every ward.</h3><p className="muted">IP-400 programmable infusion pumps designed for controlled medication delivery. Each unit is maintained under Ardent Health Network’s biomedical equipment program.</p></section><section className="card detail-meta"><p className="section-kicker">RESOURCE METADATA</p><div><span>Created</span><strong>May 18, 2026</strong></div><div><span>Updated</span><strong>Aug 30, 2026</strong></div><div><span>Expiry</span><strong>Not applicable</strong></div></section></div></main></div>; }

function AddResource({ setView }: { setView: (v: View) => void }) { return <div className="page"><Topbar title="Add resource" setView={setView} setMobileOpen={() => {}} /><main className="page-content form-page"><button className="back-button" onClick={() => setView("inventory")}><ArrowLeft size={16} /> Back to inventory</button><div className="form-intro"><p className="section-kicker">NEW RESOURCE</p><h2>Add a resource to your catalog.</h2><p className="muted">Keep your team’s shared inventory accurate and ready to act on.</p></div><section className="card form-card"><div className="form-grid"><label>Resource name<input placeholder="e.g. Portable oxygen cylinder" /></label><label>Category<select defaultValue=""><option value="" disabled>Select category</option><option>Equipment</option><option>Medicine</option><option>Blood</option><option>Medical Supply</option></select></label><label className="wide">Description<textarea placeholder="Add a short description of this resource..." /></label><label>Quantity<input placeholder="0" type="number" /></label><label>Unit<select defaultValue=""><option value="" disabled>Select unit</option><option>Units</option><option>Packs</option><option>Pairs</option><option>Liters</option></select></label><label>Availability status<select defaultValue=""><option value="" disabled>Select status</option><option>AVAILABLE</option><option>LOW STOCK</option><option>UNAVAILABLE</option></select></label><label>Condition<select defaultValue=""><option value="" disabled>Select condition</option><option>New</option><option>Excellent</option><option>Good</option><option>Needs review</option></select></label><label>Expiry date<input type="date" /></label></div><div className="form-footer"><span className="muted"><ShieldCheck size={15} /> Changes are visible to verified members.</span><div><button className="button secondary" onClick={() => setView("inventory")}>Cancel</button><button className="button primary" onClick={() => { toast.success("Resource added to your catalog."); setView("inventory"); }}>Add resource <Plus size={16} /></button></div></div></section></main></div>; }

function apiStatus(error: unknown) { return (error as { response?: { status?: number } }).response?.status; }

function requestStatusLabel(status: RequestStatus) { return status[0] + status.slice(1).toLowerCase(); }
function requestDate(value: string) { return new Date(value).toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" }); }

function Discovery({ resources, loading, error, search, category, availability, setSearch, setCategory, setAvailability, setView, setSelectedResource }: { resources: ResourceDiscoveryData[]; loading: boolean; error: string | null; search: string; category: ResourceCategory | "ALL"; availability: "ALL" | "AVAILABLE" | "LOW_STOCK"; setSearch: (value: string) => void; setCategory: (value: ResourceCategory | "ALL") => void; setAvailability: (value: "ALL" | "AVAILABLE" | "LOW_STOCK") => void; setView: (view: View) => void; setSelectedResource: (resource: ResourceDiscoveryData) => void }) {
  resources = resources.filter((resource) => resource.availability_status === "AVAILABLE");
  return <div className="page"><Topbar title="Discover resources" setView={setView} setMobileOpen={() => {}} searchQuery={search} setSearchQuery={setSearch} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">RESOURCE SHARING / VERIFIED NETWORK</p><h2>Find what your teams need.</h2><p className="muted">Explore eligible resources shared by verified healthcare organizations.</p></div><button className="button secondary" onClick={() => setView("requests")}><FileCheck2 size={16} /> My requests</button></div><div className="discovery-filters"><label>Category<select value={category} onChange={(event) => setCategory(event.target.value as ResourceCategory | "ALL")}><option value="ALL">All categories</option>{categoryOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Availability<select value={availability} onChange={(event) => setAvailability(event.target.value as "ALL" | "AVAILABLE" | "LOW_STOCK")}><option value="ALL">All availability</option><option value="AVAILABLE">Available</option><option value="LOW_STOCK">Low stock</option></select></label></div>{loading ? <section className="card discovery-list"><p className="muted">Loading shared resources...</p></section> : error ? <section className="card discovery-list"><p className="muted">{error}</p></section> : resources.length === 0 ? <section className="card discovery-list"><p className="muted">No shared resources match your search and filters.</p></section> : <section className="discovery-list">{resources.map((resource, index) => <button className="card discovery-row" key={resource.id} style={{ animationDelay: `${index * 40}ms` }} onClick={() => { setSelectedResource(resource); setView("discovery-resource"); }}><div className="resource-icon"><Activity size={17} /></div><div className="discovery-main"><strong>{resource.name}</strong><span>{categoryLabels[resource.category]} · {resource.condition ? conditionLabels[resource.condition] : "Condition not provided"}</span><small>{resource.description || "No description provided."}</small></div><div className="discovery-quantity"><strong>{resource.quantity}</strong><span>{resource.unit}</span></div><div className="discovery-provider"><strong>{resource.provider_organization_name}</strong><span>{organizationTypeLabel(resource.provider_organization_type)}</span><small>{[resource.provider_city, resource.provider_state].filter(Boolean).join(", ") || "Location not provided"}</small></div><StatusPill status={resource.availability_status === "LOW_STOCK" ? "LOW STOCK" : resource.availability_status} label={availabilityLabels[resource.availability_status]} /><ChevronRight size={16} className="row-arrow" /></button>)}</section>}</main></div>;
}

function DiscoveryResourceDetail({ resource, setView, onSubmit }: { resource: ResourceDiscoveryData | null; setView: (view: View) => void; onSubmit: (resourceId: number, quantity: number, message: string | null) => Promise<void> }) {
  const [quantity, setQuantity] = useState("1");
  const [message, setMessage] = useState("");
  const [submitting, setSubmitting] = useState(false);
  if (!resource) return <div className="page"><Topbar title="Resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><button className="back-button" onClick={() => setView("discover")}><ArrowLeft size={16} /> Back to discovery</button><section className="card"><p className="muted">This shared resource was not found.</p></section></main></div>;
  const submit = async () => { const requestedQuantity = Number(quantity); if (!Number.isInteger(requestedQuantity) || requestedQuantity <= 0) { toast.error("Requested quantity must be greater than zero."); return; } if (requestedQuantity > resource.quantity) { toast.error("Requested quantity cannot exceed the available quantity."); return; } setSubmitting(true); try { await onSubmit(resource.id, requestedQuantity, message.trim() || null); } finally { setSubmitting(false); } };
  const Icon = resourceIcon(resource.category);
  return <div className="page"><Topbar title="Shared resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content detail-page"><button className="back-button" onClick={() => setView("discover")}><ArrowLeft size={16} /> Back to discovery</button><div className="detail-heading"><div><div className="detail-icon"><Icon size={22} /></div><p className="section-kicker">{categoryLabels[resource.category]} / SHARED RESOURCE #{resource.id}</p><h2>{resource.name}</h2><p className="muted">Available from {resource.provider_organization_name}</p></div><div className="detail-actions"><StatusPill status={resource.availability_status === "LOW_STOCK" ? "LOW STOCK" : resource.availability_status} label={availabilityLabels[resource.availability_status]} /></div></div><div className="detail-stats"><div className="card detail-stat"><span>Available quantity</span><strong>{resource.quantity}</strong><small>{resource.unit}</small></div><div className="card detail-stat"><span>Condition</span><strong>{conditionLabels[resource.condition] || resource.condition}</strong><small>Provider reported condition</small></div><div className="card detail-stat"><span>Provider type</span><strong>{organizationTypeLabel(resource.provider_organization_type)}</strong><small>Verified organization</small></div><div className="card detail-stat"><span>Provider location</span><strong>{resource.provider_city || resource.provider_state || "Not provided"}</strong><small>{resource.provider_city && resource.provider_state ? resource.provider_state : "Public location information"}</small></div></div><div className="detail-columns"><section className="card detail-description"><p className="section-kicker">RESOURCE INFORMATION</p><h3>{resource.description || "No description provided."}</h3><p className="muted">{resource.condition ? `${conditionLabels[resource.condition] || resource.condition} condition · ` : ""}{resource.unit} available for a new request.</p></section><section className="card request-form-card"><p className="section-kicker">REQUEST RESOURCE</p><div className="form-grid compact"><label>Requested quantity<input type="number" min="1" max={resource.quantity} step="1" value={quantity} onChange={(event) => setQuantity(event.target.value)} /></label><label className="wide">Message <span className="field-note">Optional</span><textarea value={message} maxLength={2000} onChange={(event) => setMessage(event.target.value)} placeholder="Add context for the provider..." /></label></div><button className="button primary full" disabled={submitting} aria-busy={submitting} onClick={submit}>{submitting ? "Sending request..." : "Request resource"} <ChevronRight size={17} /></button></section></div></main></div>;
}

function NewRequest({ resources, loading, error, setView, onSubmit }: { resources: ResourceDiscoveryData[]; loading: boolean; error: string | null; setView: (view: View) => void; onSubmit: (resourceId: number, quantity: number, message: string | null) => Promise<void> }) {
  const [resourceId, setResourceId] = useState("");
  const [quantity, setQuantity] = useState("1");
  const [message, setMessage] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const selectedResource = resources.find((resource) => String(resource.id) === resourceId);
  const submit = async () => {
    const requestedQuantity = Number(quantity);
    if (!selectedResource) { toast.error("Select an available resource."); return; }
    if (!Number.isInteger(requestedQuantity) || requestedQuantity <= 0) { toast.error("Requested quantity must be greater than zero."); return; }
    if (requestedQuantity > selectedResource.quantity) { toast.error("Requested quantity cannot exceed the available quantity."); return; }
    setSubmitting(true);
    try { await onSubmit(selectedResource.id, requestedQuantity, message.trim() || null); } finally { setSubmitting(false); }
  };
  return <div className="page"><Topbar title="Generate new request" setView={setView} setMobileOpen={() => {}} /><main className="page-content form-page"><button className="back-button" onClick={() => setView("requests")}><ArrowLeft size={16} /> Back to requests</button><div className="form-intro"><p className="section-kicker">RESOURCE WORKFLOW</p><h2>Request an available resource.</h2><p className="muted">Choose from verified resources currently available across the network.</p></div><section className="card form-card">{loading ? <p className="muted">Loading available resources...</p> : error ? <p className="muted">{error}</p> : resources.length === 0 ? <p className="muted">No available resources are currently available to request.</p> : <div className="form-grid"><label className="wide">Select resource *<select value={resourceId} onChange={(event) => setResourceId(event.target.value)}><option value="">Select an available resource</option>{resources.map((resource) => <option key={resource.id} value={resource.id}>{resource.name} · {resource.provider_organization_name} · {resource.quantity} {resource.unit}</option>)}</select></label><label>Quantity *<input type="number" min="1" max={selectedResource?.quantity || undefined} step="1" value={quantity} onChange={(event) => setQuantity(event.target.value)} /></label><label className="wide">Message / reason<textarea value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Optional context for the provider" /></label><div className="form-footer wide"><span className="muted">Only available resources from other verified organizations are shown.</span><div><button className="button secondary" disabled={submitting} onClick={() => setView("requests")}>Cancel</button><button className="button primary" disabled={submitting} onClick={submit}>{submitting ? "Submitting..." : "Submit request"}</button></div></div></div>}</section></main></div>;
}

function RequestList({ requests, organization, loading, error, setView, setSelectedRequest }: { requests: ResourceRequestData[]; organization: OrganizationData | null; loading: boolean; error: string | null; setView: (view: View) => void; setSelectedRequest: (request: ResourceRequestData) => void }) {
  const ownRequests = requests.filter((request) => request.requesting_organization_id === organization?.id);
  const providedRequests = requests.filter((request) => request.providing_organization_id === organization?.id);
  const renderRequest = (request: ResourceRequestData, role: "requester" | "provider") => <button className="card request-row" key={`${role}-${request.id}`} onClick={() => { setSelectedRequest(request); setView("request"); }}><div className={`request-role ${role}`}>{role === "requester" ? "Requested by us" : "Provided by us"}</div><div className="request-main"><strong>{request.resource_name}</strong><span>{role === "requester" ? `From ${request.providing_organization_name}` : `For ${request.requesting_organization_name}`}</span>{request.message && <small>{request.message}</small>}</div><div className="request-quantity"><strong>{request.requested_quantity}</strong><span>requested</span></div><StatusPill status={request.status} label={requestStatusLabel(request.status)} /><div className="request-date">{requestDate(request.created_at)}</div><ChevronRight size={16} className="row-arrow" /></button>;
    return <div className="page"><Topbar title="My requests" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">RESOURCE WORKFLOW</p><h2>Requests that need attention.</h2><p className="muted">Track resources your organization has requested and provided.</p></div><button className="button primary" onClick={() => setView("request-create")}><Globe2 size={16} /> Generate new request</button></div>{loading ? <section className="card request-list"><p className="muted">Loading requests...</p></section> : error ? <section className="card request-list"><p className="muted">{error}</p></section> : requests.length === 0 ? <section className="card request-list"><p className="muted">No resource requests yet.</p></section> : <div className="request-sections">{ownRequests.length > 0 && <section><div className="section-heading"><div><p className="section-kicker">REQUESTER VIEW</p><h3>Requests made by {organization?.name || "your organization"}</h3></div><span className="request-count">{ownRequests.length}</span></div><div className="request-list">{ownRequests.map((request) => renderRequest(request, "requester"))}</div></section>}{providedRequests.length > 0 && <section><div className="section-heading"><div><p className="section-kicker">PROVIDER VIEW</p><h3>Requests for {organization?.name || "your organization"}</h3></div><span className="request-count">{providedRequests.length}</span></div><div className="request-list">{providedRequests.map((request) => renderRequest(request, "provider"))}</div></section>}</div>}</main></div>;
}

function RequestDetail({ request, organization, loading, error, setView, onAction, actionInProgress }: { request: ResourceRequestData | null; organization: OrganizationData | null; loading: boolean; error: string | null; setView: (view: View) => void; onAction: (action: "approve" | "reject" | "cancel" | "fulfill" | "receive") => Promise<void>; actionInProgress: string | null }) {
  if (loading) return <div className="page"><Topbar title="Request details" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><p className="muted">Loading request...</p></main></div>;
  if (error || !request) return <div className="page"><Topbar title="Request details" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><button className="back-button" onClick={() => setView("requests")}><ArrowLeft size={16} /> Back to requests</button><section className="card"><p className="muted">{error || "Request not found."}</p></section></main></div>;
  const isRequester = request.requesting_organization_id === organization?.id;
  const isProvider = request.providing_organization_id === organization?.id;
  const actionButton = (action: "approve" | "reject" | "cancel" | "fulfill" | "receive", label: string, tone: "primary" | "secondary" = "secondary") => <button className={`button ${tone}`} disabled={actionInProgress !== null} onClick={() => onAction(action)}>{actionInProgress === action ? `${label}...` : label}</button>;
  return <div className="page"><Topbar title="Request details" setView={setView} setMobileOpen={() => {}} /><main className="page-content detail-page"><button className="back-button" onClick={() => setView("requests")}><ArrowLeft size={16} /> Back to requests</button><div className="detail-heading"><div><div className="detail-icon"><FileCheck2 size={22} /></div><p className="section-kicker">RESOURCE REQUEST #{request.id}</p><h2>{request.resource_name}</h2><p className="muted">Created {requestDate(request.created_at)}</p></div><div className="detail-actions"><StatusPill status={request.status} label={requestStatusLabel(request.status)} /></div></div><div className="request-flow"><span className={request.status === "PENDING" ? "current" : "complete"}>Pending</span><ChevronRight size={15} /><span className={request.status === "APPROVED" ? "current" : ["FULFILLED", "RECEIVED"].includes(request.status) ? "complete" : ""}>Approved</span><ChevronRight size={15} /><span className={request.status === "FULFILLED" ? "current" : request.status === "RECEIVED" ? "complete" : ""}>Fulfilled</span><ChevronRight size={15} /><span className={request.status === "RECEIVED" ? "current" : ""}>Received</span>{(request.status === "REJECTED" || request.status === "CANCELLED") && <strong className="terminal-status">{requestStatusLabel(request.status)}</strong>}</div><div className="detail-stats"><div className="card detail-stat"><span>Requested quantity</span><strong>{request.requested_quantity}</strong><small>Requested units</small></div><div className="card detail-stat"><span>Requester</span><strong>{request.requesting_organization_name}</strong><small>Requesting organization</small></div><div className="card detail-stat"><span>Provider</span><strong>{request.providing_organization_name}</strong><small>Providing organization</small></div><div className="card detail-stat"><span>Last updated</span><strong>{requestDate(request.updated_at)}</strong><small>{request.status}</small></div></div><div className="detail-columns"><section className="card detail-description"><p className="section-kicker">REQUEST MESSAGE</p><h3>{request.message || "No additional message provided."}</h3><p className="muted">Resource ID #{request.resource_id}</p></section><section className="card request-actions-card"><p className="section-kicker">AVAILABLE ACTIONS</p>{isRequester && request.status === "PENDING" && actionButton("cancel", "Cancel request")} {isRequester && request.status === "FULFILLED" && actionButton("receive", "Confirm receipt", "primary")} {isProvider && request.status === "PENDING" && <div className="request-action-group">{actionButton("approve", "Approve request", "primary")}{actionButton("reject", "Reject request")}</div>} {isProvider && request.status === "APPROVED" && actionButton("fulfill", "Mark as fulfilled", "primary")} {!((isRequester && ["PENDING", "FULFILLED"].includes(request.status)) || (isProvider && ["PENDING", "APPROVED"].includes(request.status))) && <p className="muted">No actions are available for this request in its current status.</p>}</section></div></main></div>;
}

function LiveInventory({ resources, loading, error, searchQuery, setSearchQuery, setView, setSelectedResourceId }: { resources: ResourceData[]; loading: boolean; error: string | null; searchQuery: string; setSearchQuery: (value: string) => void; setView: (v: View) => void; setSelectedResourceId: (id: number) => void }) {
  const [filter, setFilter] = useState<ResourceCategory | "ALL">("ALL");
  const [availability, setAvailability] = useState<AvailabilityStatus | "ALL">("ALL");
  const [condition, setCondition] = useState<ResourceCondition | "ALL">("ALL");
  const filteredResources = resources.filter((resource) => (filter === "ALL" || resource.category === filter) && (availability === "ALL" || resource.availability_status === availability) && (condition === "ALL" || resource.condition === condition) && `${resource.name} ${resource.description || ""}`.toLowerCase().includes(searchQuery.toLowerCase()));
  return <div className="page"><Topbar title="Biomedical inventory" setView={setView} setMobileOpen={() => {}} searchQuery={searchQuery} setSearchQuery={setSearchQuery} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">RESOURCE CATALOG / {resources.length} ITEMS</p><h2>Everything in one view.</h2><p className="muted">Manage availability, condition, and expiry across your organization.</p></div><button className="button primary" onClick={() => setView("add")}><Plus size={17} /> Add resource</button></div><div className="inventory-toolbar"><div className="filter-chips"><button className={filter === "ALL" ? "selected" : ""} onClick={() => setFilter("ALL")}>All resources</button>{categoryOptions.map(([value, label]) => <button key={value} className={filter === value ? "selected" : ""} onClick={() => setFilter(value)}>{label}</button>)}</div><div className="inventory-extra-filters"><label>Availability<select value={availability} onChange={(event) => setAvailability(event.target.value as AvailabilityStatus | "ALL")}><option value="ALL">All availability</option>{availabilityOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Condition<select value={condition} onChange={(event) => setCondition(event.target.value as ResourceCondition | "ALL")}><option value="ALL">All conditions</option>{conditionOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label></div></div>{loading ? <section className="card inventory-card"><p className="muted">Loading inventory...</p></section> : error ? <section className="card inventory-card"><p className="muted">{error}</p></section> : <section className="card inventory-card"><div className="inventory-head"><span>Resource</span><span>Quantity</span><span>Status</span><span></span></div>{filteredResources.length === 0 ? <p className="muted">No resources match your filters.</p> : filteredResources.map((resource, i) => <ResourceRow key={resource.id} resource={resource} onClick={() => { setSelectedResourceId(resource.id); setView("resource"); }} delay={i} />)}</section>}</main></div>;
}

function ResourceForm({ resource, setView, onUnauthorized, onSaved }: { resource?: ResourceData; setView: (v: View) => void; onUnauthorized: () => void; onSaved: (resource: ResourceData) => Promise<void> }) {
  const [draft, setDraft] = useState<ResourceDraft>(() => resourceToDraft(resource));
  const [submitting, setSubmitting] = useState(false);
  const update = (field: keyof ResourceDraft, value: string) => setDraft((current) => ({ ...current, [field]: value }));
  const submit = async () => {
    if (!draft.name.trim() || !draft.category || !draft.quantity.trim() || !draft.unit.trim()) { toast.error("Complete the name, category, quantity, and unit."); return; }
    const quantity = Number(draft.quantity);
    if (!Number.isInteger(quantity) || quantity < 0) { toast.error("Quantity must be a whole number of zero or more."); return; }
    setSubmitting(true);
    const payload = { name: draft.name.trim(), category: draft.category, description: draft.description.trim() || null, quantity, unit: draft.unit.trim(), availability_status: draft.availability_status || "AVAILABLE", condition: draft.condition || "NEW", expiry_date: draft.expiry_date ? `${draft.expiry_date}T00:00:00Z` : null };
    try { const response = resource ? await apiClient.patch<ResourceData>(`/resources/${resource.id}`, payload) : await apiClient.post<ResourceData>("/resources", payload); await onSaved(response.data); toast.success(resource ? "Resource updated." : "Resource added to your catalog."); } catch (error) { if (apiStatus(error) === 401) onUnauthorized(); else toast.error(getApiErrorMessage(error, resource ? "Unable to update the resource." : "Unable to add the resource.")); } finally { setSubmitting(false); }
  };
  return <div className="page"><Topbar title={resource ? "Edit resource" : "Add resource"} setView={setView} setMobileOpen={() => {}} /><main className="page-content form-page"><button className="back-button" onClick={() => setView(resource ? "resource" : "inventory")}><ArrowLeft size={16} /> Back to {resource ? "resource" : "inventory"}</button><div className="form-intro"><p className="section-kicker">{resource ? "EDIT RESOURCE" : "NEW RESOURCE"}</p><h2>{resource ? "Keep this resource current." : "Add a resource to your catalog."}</h2><p className="muted">Keep your team’s shared inventory accurate and ready to act on.</p></div><section className="card form-card"><div className="form-grid"><label>Resource name<input value={draft.name} onChange={(event) => update("name", event.target.value)} placeholder="e.g. Portable oxygen cylinder" /></label><label>Category<select value={draft.category} onChange={(event) => update("category", event.target.value)}><option value="" disabled>Select category</option>{categoryOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="wide">Description<textarea value={draft.description} onChange={(event) => update("description", event.target.value)} placeholder="Add a short description of this resource..." /></label><label>Quantity<input value={draft.quantity} onChange={(event) => update("quantity", event.target.value)} placeholder="0" type="number" min="0" step="1" /></label><label>Unit<input value={draft.unit} onChange={(event) => update("unit", event.target.value)} placeholder="e.g. units" /></label><label>Availability status<select value={draft.availability_status} onChange={(event) => update("availability_status", event.target.value)}><option value="">Available</option>{availabilityOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Condition<select value={draft.condition} onChange={(event) => update("condition", event.target.value)}><option value="">New</option>{conditionOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Expiry date<input value={draft.expiry_date} onChange={(event) => update("expiry_date", event.target.value)} type="date" /></label></div><div className="form-footer"><span className="muted"><ShieldCheck size={15} /> Changes are visible to verified members.</span><div><button className="button secondary" disabled={submitting} onClick={() => setView(resource ? "resource" : "inventory")}>Cancel</button><button className="button primary" disabled={submitting} aria-busy={submitting} onClick={submit}>{submitting ? "Saving..." : resource ? "Save changes" : "Add resource"} <Plus size={16} /></button></div></div></section></main></div>;
}

function LiveResourceDetail({ resource, loading, error, setView, setEdit, onDelete, deleting }: { resource: ResourceData | null; loading: boolean; error: string | null; setView: (v: View) => void; setEdit: () => void; onDelete: () => void; deleting: boolean }) {
  if (loading) return <div className="page"><Topbar title="Resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><p className="muted">Loading resource...</p></main></div>;
  if (error || !resource) return <div className="page"><Topbar title="Resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><button className="back-button" onClick={() => setView("inventory")}><ArrowLeft size={16} /> Back to inventory</button><section className="card"><p className="muted">{error || "Resource not found."}</p></section></main></div>;
  const Icon = resourceIcon(resource.category);
  return <div className="page"><Topbar title="Resource details" setView={setView} setMobileOpen={() => {}} /><main className="page-content detail-page"><button className="back-button" onClick={() => setView("inventory")}><ArrowLeft size={16} /> Back to inventory</button><div className="detail-heading"><div><div className="detail-icon"><Icon size={22} /></div><p className="section-kicker">{categoryLabels[resource.category]} / RESOURCE #{resource.id}</p><h2>{resource.name}</h2><p className="muted">Updated {new Date(resource.updated_at).toLocaleString()}</p></div><div className="detail-actions"><StatusPill status={resourceStatus(resource.availability_status)} label={availabilityLabels[resource.availability_status]} /><button className="button secondary" onClick={setEdit}><Pencil size={16} /> Edit resource</button><button className="button secondary" disabled={deleting} onClick={onDelete}>{deleting ? "Deleting..." : "Delete"} <X size={16} /></button></div></div><div className="detail-stats"><div className="card detail-stat"><span>Quantity</span><strong>{resource.quantity}</strong><small>{resource.unit} available</small></div><div className="card detail-stat"><span>Condition</span><strong>{conditionLabels[resource.condition]}</strong><small>Current condition</small></div><div className="card detail-stat"><span>Active status</span><strong>{resource.is_active ? "Active" : "Inactive"}</strong><small>{resource.is_active ? "Visible to organization" : "Soft deleted"}</small></div><div className="card detail-stat"><span>Expiry</span><strong>{formatExpiry(resource.expiry_date)}</strong><small>{resource.expiry_date ? "Recorded expiry date" : "No expiry recorded"}</small></div></div><div className="detail-columns"><section className="card detail-description"><p className="section-kicker">DESCRIPTION</p><h3>{resource.description || "No description provided."}</h3><p className="muted">This resource is managed within your organization’s biomedical inventory.</p></section><section className="card detail-meta"><p className="section-kicker">RESOURCE METADATA</p><div><span>Created</span><strong>{new Date(resource.created_at).toLocaleDateString()}</strong></div><div><span>Updated</span><strong>{new Date(resource.updated_at).toLocaleDateString()}</strong></div><div><span>Category</span><strong>{categoryLabels[resource.category]}</strong></div></section></div></main></div>;
}

function Organization({ setView, organization, loading, setOrganization, onUnauthorized }: { setView: (v: View) => void; organization: OrganizationData | null; loading: boolean; setOrganization: (organization: OrganizationData) => void; onUnauthorized: () => void }) {
  const [editing, setEditing] = useState(false);
  const [isSaving, setIsSaving] = useState(false);
  const [draft, setDraft] = useState<Partial<OrganizationData>>({});

  useEffect(() => { if (organization) setDraft(organization); }, [organization]);

  if (loading || !organization) return <div className="page"><Topbar title="Organization" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><p className="muted">Loading organization...</p></main></div>;

  const saveOrganization = async () => {
    setIsSaving(true);
    try {
      const { data } = await apiClient.patch<OrganizationData>(`/organizations/${organization.id}`, {
        name: draft.name,
        registration_number: draft.registration_number,
        organization_type: draft.organization_type,
        email: draft.email || null,
        phone: draft.phone || null,
        address: draft.address || null,
        city: draft.city || null,
        state: draft.state || null,
        pincode: draft.pincode || null,
      });
      const refreshed = await apiClient.get<OrganizationData>("/organizations/me");
      setOrganization(refreshed.data || data);
      setEditing(false);
      toast.success("Organization profile updated.");
    } catch (error) {
      if ((error as { response?: { status?: number } }).response?.status === 401) onUnauthorized();
      else toast.error(getApiErrorMessage(error, "Unable to update the organization."));
    } finally { setIsSaving(false); }
  };

  const setDraftField = (field: keyof OrganizationData, value: string) => setDraft((current) => ({ ...current, [field]: value }));
  const address = [organization.address, organization.city, organization.state, organization.pincode].filter(Boolean).join(", ") || "Not provided";
  return <div className="page"><Topbar title="Organization" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="intro-row"><div><p className="section-kicker">ORGANIZATION PROFILE</p><h2>{organization.name}</h2><p className="muted">The verified identity behind your resource catalog.</p></div><button className="button secondary" disabled={isSaving} onClick={() => setEditing((current) => !current)}><Pencil size={16} /> {editing ? "Cancel" : "Edit profile"}</button></div><section className="org-hero card"><div className="org-symbol"><Building2 size={26} /></div><div><h3>{organization.name}</h3><p className="muted">{organizationTypeLabel(organization.organization_type)} · {[organization.city, organization.state].filter(Boolean).join(", ") || "Location not provided"}</p></div><StatusPill status={organization.verification_status} label={verificationLabel(organization.verification_status)} /></section><div className="org-grid"><section className="card info-card"><p className="section-kicker">ORGANIZATION DETAILS</p>{editing ? <><label className="form-grid"><span>Name<input value={draft.name || ""} onChange={(event) => setDraftField("name", event.target.value)} /></span><span>Registration number<input value={draft.registration_number || ""} onChange={(event) => setDraftField("registration_number", event.target.value)} /></span><span>Email<input value={draft.email || ""} onChange={(event) => setDraftField("email", event.target.value)} /></span><span>Phone<input value={draft.phone || ""} onChange={(event) => setDraftField("phone", event.target.value)} /></span><span>Address<input value={draft.address || ""} onChange={(event) => setDraftField("address", event.target.value)} /></span><span>City<input value={draft.city || ""} onChange={(event) => setDraftField("city", event.target.value)} /></span><span>State<input value={draft.state || ""} onChange={(event) => setDraftField("state", event.target.value)} /></span><span>Pincode<input value={draft.pincode || ""} onChange={(event) => setDraftField("pincode", event.target.value)} /></span></label><button className="button primary" disabled={isSaving} onClick={saveOrganization}>{isSaving ? "Saving..." : "Save changes"}</button></> : [["Registration number", organization.registration_number], ["Organization type", organizationTypeLabel(organization.organization_type)], ["Email", organization.email || "Not provided"], ["Phone", organization.phone || "Not provided"], ["Address", organization.address || "Not provided"], ["City / State", address]].map(([a, b]) => <div className="info-line" key={a}><span>{a}</span><strong>{b}</strong></div>)}</section><section className="card verification-card"><p className="section-kicker">VERIFICATION STATUS</p><div className="verify-ring"><ShieldCheck size={28} /></div><h3>{verificationLabel(organization.verification_status)} organization</h3><p className="muted">Your organization is currently {verificationLabel(organization.verification_status).toLowerCase()}. You can manage biomedical resources for your network.</p><button className="text-button" onClick={() => toast.info("Verification details are available to administrators.")}>View verification details <ChevronRight size={15} /></button></section></div></main></div>;
}

function SettingsPage({ setView }: { setView: (v: View) => void }) { const { user } = useAccount(); const displayName = userDisplayName(user); return <div className="page"><Topbar title="Settings" setView={setView} setMobileOpen={() => {}} /><main className="page-content"><div className="form-intro"><p className="section-kicker">ACCOUNT & PREFERENCES</p><h2>Settings</h2><p className="muted">Manage your profile and notification preferences.</p></div><div className="settings-grid"><section className="card settings-card"><p className="section-kicker">PROFILE INFORMATION</p><div className="profile-large"><div className="avatar avatar-lg">{userInitials(user)}</div><div><h3>{displayName}</h3><p className="muted">{userRoleLabel(user)}</p></div><button className="button secondary small" onClick={() => toast.info("Profile editing is coming soon.")}>Edit</button></div>{[["Full name", displayName],["Email", user?.email || "Not available"],["Role", userRoleLabel(user)]].map(([a,b]) => <div className="info-line" key={a}><span>{a}</span><strong>{b}</strong></div>)}</section><section className="card settings-card"><p className="section-kicker">ACCOUNT SETTINGS</p><div className="setting-row"><div><strong>Email notifications</strong><span>Receive updates about low stock</span></div><div className="toggle on"><span /></div></div><div className="setting-row"><div><strong>Weekly inventory digest</strong><span>A Monday summary for your network</span></div><div className="toggle on"><span /></div></div><div className="setting-row"><div><strong>Security alerts</strong><span>Important access and profile changes</span></div><div className="toggle on"><span /></div></div></section></div></main></div>; }

function Login({ setView, onAuthenticated }: { setView: (v: View) => void; onAuthenticated: () => Promise<void> }) {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [rememberMe, setRememberMe] = useState(true);
  const [isSubmitting, setIsSubmitting] = useState(false);

  const handleLogin = async () => {
    if (!email.trim() || !password) {
      toast.error("Enter your email and password.");
      return;
    }

    setIsSubmitting(true);
    try {
      await login(email, password, rememberMe);
      toast.success("Welcome back.");
      await onAuthenticated();
    } catch (error) {
      toast.error(getApiErrorMessage(error, "Unable to sign in. Please try again."));
    } finally {
      setIsSubmitting(false);
    }
  };

  return <div className="auth-page"><div className="auth-visual"><div className="auth-brand"><div className="brand-mark"><img src={logo} alt="" /></div><span>MEDLINK</span></div><div className="auth-art"><img src={authArt} alt="Connected medical cross illustration" /></div><div className="auth-quote"><p>“Clarity is care in motion.”</p><span>MedLink for verified healthcare organizations.</span></div></div><div className="auth-form"><div className="auth-form-inner"><p className="section-kicker">WELCOME BACK</p><h1>Sign in to MedLink.</h1><p className="muted">Manage what matters across your healthcare network.</p><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} /></label><div className="form-inline"><label className="check"><input type="checkbox" checked={rememberMe} onChange={(event) => setRememberMe(event.target.checked)} /> Remember me</label><button className="text-button" onClick={() => toast.info("Password reset is coming soon.")}>Forgot password?</button></div><button className="button primary full" disabled={isSubmitting} aria-busy={isSubmitting} onClick={handleLogin}>{isSubmitting ? "Signing in..." : "Sign in"} <ChevronRight size={17} /></button><p className="auth-switch">New to MedLink? <button onClick={() => setView("onboarding")}>Create an account</button></p></div></div></div>;
}

function Onboarding({ setView, setupOnly, onCreated }: { setView: (v: View) => void; setupOnly: boolean; onCreated: (organization: OrganizationData) => void }) {
  const [fullName, setFullName] = useState("");
  const [password, setPassword] = useState("");
  const [organizationName, setOrganizationName] = useState("");
  const [registrationNumber, setRegistrationNumber] = useState("");
  const [organizationType, setOrganizationType] = useState("");
  const [email, setEmail] = useState("");
  const [isSubmitting, setIsSubmitting] = useState(false);

  const submitOnboarding = async () => {
    if ((!setupOnly && (!fullName.trim() || !password || !email.trim())) || !organizationName.trim() || !registrationNumber.trim() || !organizationType) {
      toast.error(setupOnly ? "Complete the organization details." : "Complete your account and organization details.");
      return;
    }
    if (!setupOnly && password.length < 8) { toast.error("Password must be at least 8 characters."); return; }

    setIsSubmitting(true);
    try {
      if (!setupOnly) {
        await registerUser({ email, password, full_name: fullName });
        await login(email, password);
      }
      await apiClient.post("/organizations", { name: organizationName, registration_number: registrationNumber, organization_type: organizationTypeValues[organizationType], email: email || null });
      const { data } = await apiClient.get<OrganizationData>("/organizations/me");
      onCreated(data);
      toast.success("Organization created.");
      setView("organization");
    } catch (error) {
      if ((error as { response?: { status?: number } }).response?.status === 401) { clearAccessToken(); setView("login"); }
      else toast.error(getApiErrorMessage(error, "Unable to complete onboarding."));
    } finally { setIsSubmitting(false); }
  };

  return <div className="auth-page"><div className="auth-visual onboarding-visual"><div className="auth-brand"><div className="brand-mark"><img src={logo} alt="" /></div><span>MEDLINK</span></div><div className="onboarding-copy"><p className="section-kicker">STEP 02 / ORGANIZATION</p><h1>Bring your organization into view.</h1><p>Verified healthcare teams use MedLink to keep biomedical resources clear, current, and ready.</p><div className="onboarding-steps"><span className="done">01</span><span className="line" /><span className="current">02</span><span className="line" /><span>03</span></div></div></div><div className="auth-form"><div className="auth-form-inner"><p className="section-kicker">{setupOnly ? "REGISTER ORGANIZATION" : "CREATE YOUR ACCOUNT"}</p><h1>{setupOnly ? "Tell us about your team." : "Create your MedLink workspace."}</h1><p className="muted">We’ll review these details before activating your workspace.</p><div className="form-grid compact">{!setupOnly && <><label className="wide">Full name<input value={fullName} onChange={(event) => setFullName(event.target.value)} placeholder="e.g. Jordan Davis" /></label><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@organization.org" /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="At least 8 characters" /></label></>}<label className="wide">Organization name<input value={organizationName} onChange={(event) => setOrganizationName(event.target.value)} placeholder="e.g. Ardent Health Network" /></label><label>Registration number<input value={registrationNumber} onChange={(event) => setRegistrationNumber(event.target.value)} placeholder="Registration ID" /></label><label>Organization type<select value={organizationType} onChange={(event) => setOrganizationType(event.target.value)}><option value="" disabled>Select type</option>{Object.keys(organizationTypeValues).map((label) => <option key={label}>{label}</option>)}</select></label></div><button className="button primary full" disabled={isSubmitting} onClick={submitOnboarding}>{isSubmitting ? (setupOnly ? "Creating organization..." : "Creating account...") : "Submit for verification"} <ChevronRight size={17} /></button><p className="auth-switch">Already have an account? <button onClick={() => setView("login")}>Sign in</button></p></div></div></div>;
}

export default function App() {
  const [view, setView] = useState<View>("login");
  const [authInitialized, setAuthInitialized] = useState(false);
  const [user, setUser] = useState<User | null>(null);
  const sessionGeneration = useRef(0);
  const [mobileOpen, setMobileOpen] = useState(false);
  const [organization, setOrganization] = useState<OrganizationData | null>(null);
  const [organizationLoading, setOrganizationLoading] = useState(false);
  const [setupOnly, setSetupOnly] = useState(false);
  const [resources, setResources] = useState<ResourceData[]>([]);
  const [selectedResourceId, setSelectedResourceId] = useState<number | null>(null);
  const [resourceDetail, setResourceDetail] = useState<ResourceData | null>(null);
  const [resourceLoading, setResourceLoading] = useState(false);
  const [inventoryLoading, setInventoryLoading] = useState(false);
  const [resourceError, setResourceError] = useState<string | null>(null);
  const [searchQuery, setSearchQuery] = useState("");
  const [deleting, setDeleting] = useState(false);
  const [discoveryResources, setDiscoveryResources] = useState<ResourceDiscoveryData[]>([]);
  const [discoveryResource, setDiscoveryResource] = useState<ResourceDiscoveryData | null>(null);
  const [discoveryLoading, setDiscoveryLoading] = useState(false);
  const [discoveryError, setDiscoveryError] = useState<string | null>(null);
  const [discoverySearch, setDiscoverySearch] = useState("");
  const [discoveryCategory, setDiscoveryCategory] = useState<ResourceCategory | "ALL">("ALL");
  const [discoveryAvailability, setDiscoveryAvailability] = useState<"ALL" | "AVAILABLE" | "LOW_STOCK">("ALL");
  const [requests, setRequests] = useState<ResourceRequestData[]>([]);
  const [selectedRequestId, setSelectedRequestId] = useState<number | null>(null);
  const [requestDetail, setRequestDetail] = useState<ResourceRequestData | null>(null);
  const [requestsLoading, setRequestsLoading] = useState(false);
  const [requestLoading, setRequestLoading] = useState(false);
  const [requestError, setRequestError] = useState<string | null>(null);
  const [requestAction, setRequestAction] = useState<string | null>(null);

  useEffect(() => { const openNav = () => setMobileOpen(true); window.addEventListener("medlink:open-nav", openNav); return () => window.removeEventListener("medlink:open-nav", openNav); }, []);
  const loadOrganization = async (generation = sessionGeneration.current) => { setOrganizationLoading(true); try { const { data } = await apiClient.get<OrganizationData>("/organizations/me"); if (generation !== sessionGeneration.current) return false; setOrganization(data); setSetupOnly(false); return true; } catch (error) { if (generation !== sessionGeneration.current) return false; const status = apiStatus(error); if (status === 404) { setSetupOnly(true); return false; } if (status === 401) handleLogout(false); else toast.error(getApiErrorMessage(error, "Unable to load the organization.")); return false; } finally { if (generation === sessionGeneration.current) setOrganizationLoading(false); } };
  const clearAccountState = () => { setUser(null); setOrganization(null); setResources([]); setSelectedResourceId(null); setResourceDetail(null); setResourceError(null); setDiscoveryResources([]); setDiscoveryResource(null); setDiscoveryError(null); setDiscoverySearch(""); setDiscoveryCategory("ALL"); setDiscoveryAvailability("ALL"); setRequests([]); setSelectedRequestId(null); setRequestDetail(null); setRequestError(null); setInventoryLoading(false); setResourceLoading(false); setDiscoveryLoading(false); setRequestsLoading(false); setRequestLoading(false); setRequestAction(null); setSearchQuery(""); setSetupOnly(false); };
  const handleLogout = (notify = true) => { sessionGeneration.current += 1; clearAccessToken(); clearAccountState(); setView("login"); setAuthInitialized(true); setMobileOpen(false); if (notify) toast.success("You have been signed out."); };
  const loadResources = async (generation = sessionGeneration.current) => { setInventoryLoading(true); setResourceError(null); try { const { data } = await apiClient.get<ResourceData[]>("/resources"); if (generation === sessionGeneration.current) setResources(data); } catch (error) { if (generation !== sessionGeneration.current) return; if (apiStatus(error) === 401) handleLogout(); else { const message = getApiErrorMessage(error, "Unable to load inventory."); setResourceError(message); toast.error(message); } } finally { if (generation === sessionGeneration.current) setInventoryLoading(false); } };
  const loadResourceDetail = async (id: number, generation = sessionGeneration.current) => { setResourceLoading(true); setResourceError(null); try { const { data } = await apiClient.get<ResourceData>(`/resources/${id}`); if (generation === sessionGeneration.current) setResourceDetail(data); } catch (error) { if (generation !== sessionGeneration.current) return; if (apiStatus(error) === 401) handleLogout(); else { const message = apiStatus(error) === 403 ? "You do not have access to this resource." : apiStatus(error) === 404 ? "This resource was not found." : getApiErrorMessage(error, "Unable to load the resource."); setResourceError(message); toast.error(message); setResourceDetail(null); } } finally { if (generation === sessionGeneration.current) setResourceLoading(false); } };
  const loadDiscoveryResources = async (generation = sessionGeneration.current) => { setDiscoveryLoading(true); setDiscoveryError(null); try { const params: Record<string, string> = { availability_status: "AVAILABLE" }; if (discoverySearch.trim()) params.search = discoverySearch.trim(); if (discoveryCategory !== "ALL") params.category = discoveryCategory; const { data } = await apiClient.get<ResourceDiscoveryData[]>("/resource-sharing/resources", { params }); if (generation === sessionGeneration.current) setDiscoveryResources(data.filter((resource) => resource.availability_status === "AVAILABLE")); } catch (error) { if (generation !== sessionGeneration.current) return; const status = apiStatus(error); if (status === 401) handleLogout(); else { const message = status === 403 ? "Resource sharing is available to active, verified organizations only." : getApiErrorMessage(error, "Unable to discover shared resources."); setDiscoveryError(message); toast.error(message); } } finally { if (generation === sessionGeneration.current) setDiscoveryLoading(false); } };
  const loadRequests = async (generation = sessionGeneration.current) => { setRequestsLoading(true); setRequestError(null); try { const { data } = await apiClient.get<ResourceRequestData[]>("/resource-requests"); if (generation === sessionGeneration.current) setRequests(data); } catch (error) { if (generation !== sessionGeneration.current) return; const status = apiStatus(error); if (status === 401) handleLogout(); else { const message = status === 403 ? "Requests are available to active, verified organizations only." : getApiErrorMessage(error, "Unable to load resource requests."); setRequestError(message); toast.error(message); } } finally { if (generation === sessionGeneration.current) setRequestsLoading(false); } };
  const loadRequestDetail = async (id: number, generation = sessionGeneration.current) => { setRequestLoading(true); setRequestError(null); try { const { data } = await apiClient.get<ResourceRequestData>(`/resource-requests/${id}`); if (generation === sessionGeneration.current) setRequestDetail(data); } catch (error) { if (generation !== sessionGeneration.current) return; const status = apiStatus(error); if (status === 401) handleLogout(); else { const message = status === 403 ? "You do not have access to this request." : status === 404 ? "This request was not found." : getApiErrorMessage(error, "Unable to load the request."); setRequestError(message); toast.error(message); setRequestDetail(null); } } finally { if (generation === sessionGeneration.current) setRequestLoading(false); } };
  useEffect(() => { if (view === "dashboard" || view === "inventory") loadResources(); }, [view]);
  useEffect(() => { if (view === "resource" && selectedResourceId !== null) loadResourceDetail(selectedResourceId); }, [view, selectedResourceId]);
  useEffect(() => { if (view === "discover" || view === "request-create") loadDiscoveryResources(); }, [view, discoverySearch, discoveryCategory, discoveryAvailability]);
  useEffect(() => { if (view === "requests") loadRequests(); }, [view]);
  useEffect(() => { if (view === "request" && selectedRequestId !== null) loadRequestDetail(selectedRequestId); }, [view, selectedRequestId]);
  useEffect(() => { const token = getAccessToken(); if (!token) { setAuthInitialized(true); return; } const generation = sessionGeneration.current; getCurrentUser().then((authenticatedUser) => { if (generation !== sessionGeneration.current) return; setUser(authenticatedUser); return loadOrganization(generation); }).then((hasOrganization) => { if (generation === sessionGeneration.current && hasOrganization !== undefined) setView(hasOrganization ? "dashboard" : "onboarding"); }).catch(() => { if (generation === sessionGeneration.current) handleLogout(false); }).finally(() => { if (generation === sessionGeneration.current) setAuthInitialized(true); }); }, []);
  const handleAuthenticated = async () => { const generation = ++sessionGeneration.current; const authenticatedUser = await getCurrentUser(); if (generation !== sessionGeneration.current) return; setUser(authenticatedUser); const hasOrganization = await loadOrganization(generation); if (generation === sessionGeneration.current) setView(hasOrganization ? "dashboard" : "onboarding"); };
  const handleOrganizationCreated = (createdOrganization: OrganizationData) => { setOrganization(createdOrganization); setSetupOnly(false); };
  const handleResourceSaved = async (savedResource: ResourceData) => { setResourceDetail(savedResource); await loadResources(); setSelectedResourceId(savedResource.id); setView(view === "edit" ? "resource" : "inventory"); };
  const handleRequestCreated = async (resourceId: number, quantity: number, message: string | null) => { try { const { data } = await apiClient.post<ResourceRequestData>("/resource-requests", { resource_id: resourceId, requested_quantity: quantity, message }); setRequestDetail(data); setSelectedRequestId(data.id); await loadRequests(); toast.success("Resource request submitted."); setView("request"); } catch (error) { const status = apiStatus(error); if (status === 401) handleLogout(); else toast.error(getApiErrorMessage(error, status === 400 ? "Unable to submit this request." : "Unable to submit the resource request.")); } };
  const handleRequestAction = async (action: "approve" | "reject" | "cancel" | "fulfill" | "receive") => { if (!requestDetail) return; setRequestAction(action); try { const { data } = await apiClient.post<ResourceRequestData>(`/resource-requests/${requestDetail.id}/${action}`); setRequestDetail(data); setRequests((current) => current.map((request) => request.id === data.id ? data : request)); await loadRequests(); toast.success(`Request ${requestStatusLabel(data.status).toLowerCase()}.`); } catch (error) { const status = apiStatus(error); if (status === 401) handleLogout(); else if (status === 403) toast.error("You do not have permission to perform that action."); else if (status === 404) toast.error("This request was not found."); else toast.error(getApiErrorMessage(error, "Unable to update the request.")); } finally { setRequestAction(null); } };
  const handleDelete = async () => { if (!resourceDetail || !window.confirm(`Delete ${resourceDetail.name} from active inventory?`)) return; setDeleting(true); try { await apiClient.delete(`/resources/${resourceDetail.id}`); await loadResources(); setResourceDetail(null); setView("inventory"); toast.success("Resource removed from active inventory."); } catch (error) { if (apiStatus(error) === 401) handleLogout(); else toast.error(getApiErrorMessage(error, "Unable to delete the resource.")); } finally { setDeleting(false); } };
  const content = view === "dashboard" ? <Dashboard resources={resources} organization={organization} loading={inventoryLoading} error={resourceError} setView={setView} setSelectedResourceId={setSelectedResourceId} /> : view === "inventory" ? <LiveInventory resources={resources} loading={inventoryLoading} error={resourceError} searchQuery={searchQuery} setSearchQuery={setSearchQuery} setView={setView} setSelectedResourceId={setSelectedResourceId} /> : view === "resource" ? <LiveResourceDetail resource={resourceDetail} loading={resourceLoading} error={resourceError} setView={setView} setEdit={() => setView("edit")} onDelete={handleDelete} deleting={deleting} /> : view === "add" ? <ResourceForm setView={setView} onUnauthorized={handleLogout} onSaved={handleResourceSaved} /> : view === "edit" ? <ResourceForm resource={resourceDetail || undefined} setView={setView} onUnauthorized={handleLogout} onSaved={handleResourceSaved} /> : view === "organization" ? <Organization setView={setView} organization={organization} loading={organizationLoading} setOrganization={setOrganization} onUnauthorized={handleLogout} /> : view === "settings" ? <SettingsPage setView={setView} /> : view === "discover" ? <Discovery resources={discoveryResources} loading={discoveryLoading} error={discoveryError} search={discoverySearch} category={discoveryCategory} availability={discoveryAvailability} setSearch={setDiscoverySearch} setCategory={setDiscoveryCategory} setAvailability={setDiscoveryAvailability} setView={setView} setSelectedResource={setDiscoveryResource} /> : view === "request-create" ? <NewRequest resources={discoveryResources} loading={discoveryLoading} error={discoveryError} setView={setView} onSubmit={async (resourceId, quantity, message) => { await handleRequestCreated(resourceId, quantity, message); setView("requests"); }} /> : view === "discovery-resource" ? <DiscoveryResourceDetail resource={discoveryResource} setView={setView} onSubmit={handleRequestCreated} /> : view === "requests" ? <RequestList requests={requests} organization={organization} loading={requestsLoading} error={requestError} setView={setView} setSelectedRequest={(request) => { setRequestDetail(request); setSelectedRequestId(request.id); }} /> : view === "request" ? <RequestDetail request={requestDetail} organization={organization} loading={requestLoading} error={requestError} setView={setView} onAction={handleRequestAction} actionInProgress={requestAction} /> : view === "login" ? <Login setView={setView} onAuthenticated={handleAuthenticated} /> : <Onboarding setView={setView} setupOnly={setupOnly} onCreated={handleOrganizationCreated} />;
  if (!authInitialized) return <div className="auth-page"><div className="auth-form"><p className="muted">Checking existing session...</p></div></div>;
  const account = <AccountContext.Provider value={{ user, organization }}>{content}</AccountContext.Provider>;
  return view === "login" || view === "onboarding" ? account : <AccountContext.Provider value={{ user, organization }}><div className="app-shell"><Sidebar view={view} setView={setView} mobileOpen={mobileOpen} setMobileOpen={setMobileOpen} onLogout={handleLogout} resourceCount={resources.length} resourcesLoading={inventoryLoading} />{content}</div></AccountContext.Provider>;
}
